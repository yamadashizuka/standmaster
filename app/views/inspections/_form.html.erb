<%= form_for(@inspection) do |f| %>
  <% if @inspection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inspection.errors.count, "error") %> prohibited this inspection from being saved:</h2>

      <ul>
      <% @inspection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mechanic %><br>
    <%= f.text_field :mechanic %>
  </div>
  <div class="field">
    <%= f.label :memo %><br>
    <%= f.text_area :memo %>
  </div>
  <div class="field">
    <%= f.label :visit_id %><br>
    <%= f.number_field :visit_id %>
  </div>

  <h2>エンジンルームの点検</h2>
<%= f.fields_for :engineroom do |engineroom_fields| %>
    <%= engineroom_fields.number_field :noise_id %>
    <%= engineroom_fields.number_field :oilcolor_id %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
